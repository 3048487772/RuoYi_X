# 代码生成
gen: 
  # 作者
  author: wdf
  # 默认生成包路径 system 需改成自己的模块名称 如 system monitor tool
  packageName: com.ruoyi.bis
  # 自动去除表前缀，默认是false
  autoRemovePre: true
  # 表前缀（生成类名不会包含表前缀，多个用逗号分隔）
  tablePrefix: sys_,bis_
  # 生成代码时后端名称
  backModuleName: ruoyi-bussiness
  # 生成代码时前端名称
  frontModuleName: ruoyi-ui
